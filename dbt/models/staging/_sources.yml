version: 2

sources:
  - name: raw
    database: "{{ env_var('GCP_PROJECT_ID') }}"
    schema: "{{ env_var('BQ_DATASET_RAW', 'cripto_raw') }}"
    tables:
      - name: coingecko_market_data
        description: "Snapshot diario de mercado das criptos (camada raw)."
